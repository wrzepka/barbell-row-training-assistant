# This workflow will install Python dependencies and run tests with Python 3.12 using GitHub-hosted runners

name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    # ZMIANA: Zamiast self-hosted, prosimy GitHub o ich maszynę z Windowsem
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        # Składnia PowerShell dla maszyn Windows na GitHubie
        if (Test-Path requirements.txt) { pip install -r requirements.txt }
        
    - name: Test with pytest
      run: |
        pytest
